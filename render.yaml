services:
  - type: web
    name: stern-voice-bot
    runtime: docker
    plan: free
    envVars:
      - key: ELEVENLABS_API_KEY
        sync: false
      - key: TELEGRAM_BOT_TOKEN
        sync: false
